# Placeholder — chưa có code Go để build (README §"Trạng thái", bước 4 API
# backend chưa bắt đầu, xem docs/schema.md). Viết lại multi-stage build (Go
# build stage + distroless/alpine runtime) khi có go.mod + cmd/server.
